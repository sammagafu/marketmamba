# Copy to .env.development (or .env.development.local) and restart `npm run dev`.
#
# Local Docker (recommended — run: docker compose -p marketmamba up -d)
VITE_API_PROXY_TARGET=http://127.0.0.1:8090
#
# Production VPS (when https://marketmamba.kkooapp.co.tz works):
# VITE_API_PROXY_TARGET=https://marketmamba.kkooapp.co.tz
