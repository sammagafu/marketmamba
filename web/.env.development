# Vite proxies /api → this URL. Restart npm run dev after changes.
# Local Docker (default):
VITE_API_PROXY_TARGET=http://127.0.0.1:8090
# Production VPS (only when nginx + app are up):
# VITE_API_PROXY_TARGET=https://marketmamba.kkooapp.co.tz
